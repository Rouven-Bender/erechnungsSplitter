create table account (
    id integer primary key,
    accountnumber char(5),
    aname varchar(255)
);
create table personenkonto (
    id integer primary key,
    kontonumber char(6),
    aname varchar(255)
)